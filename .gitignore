**/certs/grpc/*.crt
**/certs/grpc/*.key
.orbit/
.DS_Store

# Compiled binary (only at root, not the cmd/sprinqua/ source dir)
/sprinqua

# Runtime files (generated by the app)
cmd/config.json
cmd/history.json
cmd/sprinqua/config.json
cmd/sprinqua/history.json

# OrbitOS SDK (local replace — not part of the app)
orbit-os-sdk-go/

# Go workspace (local dev only)
go.work
go.work.sum

# Build artifacts
*.orb
dist/
